# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate node_resolver

Name:           rust-node_resolver
Version:        0.66.0
Release:        %autorelease
Summary:        Node.js module resolution algorithm used in Deno

License:        MIT
URL:            https://crates.io/crates/node_resolver
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Node.js module resolution algorithm used in Deno.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
# FIXME: no license files detected
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+deno_graph-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+deno_graph-devel %{_description}

This package contains library source intended for building other packages which
use the "deno_graph" feature of the "%{crate}" crate.

%files       -n %{name}+deno_graph-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+graph-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+graph-devel %{_description}

This package contains library source intended for building other packages which
use the "graph" feature of the "%{crate}" crate.

%files       -n %{name}+graph-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+sync-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+sync-devel %{_description}

This package contains library source intended for building other packages which
use the "sync" feature of the "%{crate}" crate.

%files       -n %{name}+sync-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
